# =============================================================================
# DEVELOPMENT / STAGING OVERRIDES
# -----------------------------------------------------------------------------
# Intended for shared development or staging environments. Replace the example
# values with the ones that match your infrastructure.
# Apply with: php artisan env:use development
# =============================================================================

APP_ENV=development
APP_DEBUG=true
APP_URL=https://dev.client-example.test

IAM_HOST=https://iam.dev.client-example.test
IAM_VERIFY_ENDPOINT=${IAM_HOST}/api/sso/verify

SESSION_DOMAIN=.dev.client-example.test

LOG_LEVEL=debug

MAIL_MAILER=smtp
MAIL_HOST=smtp.dev.client-example.test
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
