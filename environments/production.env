# =============================================================================
# PRODUCTION OVERRIDES
# -----------------------------------------------------------------------------
# Review each value before deploying to production. Apply with:
#   php artisan env:use production
# =============================================================================

APP_ENV=production
APP_DEBUG=false
APP_URL=https://client.example.com

IAM_HOST=https://iam.client-example.com
IAM_VERIFY_ENDPOINT=${IAM_HOST}/api/sso/verify

SESSION_DOMAIN=.client-example.com

LOG_LEVEL=warning

MAIL_MAILER=smtp
MAIL_HOST=smtp.client-example.com
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
